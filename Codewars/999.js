function arrayPlusArray(arr1, arr2) {
    let arr = arr1.concat(arr2);
    let sum = 0;
    for (i = 0; i < arr.length; i++) {
      sum += arr.length[i];
    }
    return sum;
  }
  arrayPlusArray([1, 2], [1, 2]);